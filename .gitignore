.DS_Store

# Leftover PlatformIO build cache / auto-generated IDE config from before the
# switch to a plain Arduino sketch — regenerates per-machine, never tracked.
.pio/
.vscode/c_cpp_properties.json
.vscode/launch.json

# Hardware bring-up / diagnostic sketches — not part of the tracked firmware
bringup/
